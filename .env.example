# Database Configuration
DB_NAME=smarttax
DB_USER=root
DB_PASS=
DB_HOST=localhost

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (Generate a random string for production)
JWT_SECRET=smarttax_secret

# CORS Configuration (optional)
# FRONTEND_URL=http://localhost:5173
